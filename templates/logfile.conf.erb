LoadPlugin "logfile"
<Plugin "logfile">
  LogLevel "debug"
  File "/opt/stack/collectd.log"
  Timestamp true
</Plugin>

# Decrease syslog verbosity, to avoid duplicate logging
<Plugin "syslog">
  LogLevel "err"
</Plugin>
